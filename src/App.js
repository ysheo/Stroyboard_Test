import * as React from 'react';
import { Reset } from 'styled-reset';
import './App.css';

const App = () => (
  <React.Fragment>
    <Reset />
    <div>Hi, I'm an dog!</div>
  </React.Fragment>
);

export default App;
